// use merge sort to sort file by time and delete entries that are invalid

// define header with data type
// during the sort, if row doesn't match header -> remove
// else -> save in object as { 'lat/lng': {everything else} }